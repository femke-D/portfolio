---
import "../styles/global.css";
import video from "../assets/gardigan.mp4";
import promo from "../assets/promo.mp4";
import casemovie from "../assets/casemovie.mp4";

const { title, description, tools, services, screen } = Astro.props;
---

<div class="markdown-layout">
    <a href="/portfolio/project" class="back-button"> Back to projects </a>

    <div class="project-header">
        <div class="project__image-wrapper">
            {
                screen ? (
                    <img src={screen.src} alt={title} class="project__image" />
                ) : (
                    <p>Geen afbeelding beschikbaar</p>
                )
            }
        </div>

        <h1 class="project__title">{title}</h1>
    </div>

    <div class="project-body-wrapper">
        <div class="project-info">
            <div class="info-row">
                <p class="project__description">
                    <strong class="word__category">Category:</strong>
                    <br />
                    {description}
                </p>

                <p class="project__description">
                    <strong class="word__tools">Tools:</strong>
                    <br />
                    {tools}
                </p>
            </div>

            <p class="project__description">
                <strong class="word__services">Services:</strong>
                <br />
                {services}
            </p>
        </div>

        <div class="project__content">
            <slot />

            {
                title === "Gardigan coffee" && (
                    <video
                        class="video_gardigan"
                        width="60%"
                        height="auto"
                        controls
                        muted
                        playsinline
                        preload="metadata"
                        class="project-video"
                    >
                        <source src={video} type="video/mp4" />
                    </video>
                )
            }
            {
                title === "The portal" && (
                    <div class="portal-videos">
                        <video
                            class="video_portal"
                            controls
                            muted
                            playsinline
                            preload="metadata"
                        >
                            <source src={promo} type="video/mp4" />
                        </video>

                        <video
                            class="video_portal"
                            controls
                            muted
                            playsinline
                            preload="metadata"
                        >
                            <source src={casemovie} type="video/mp4" />
                        </video>
                    </div>
                )
            }
        </div>
    </div>
</div>

<style>
    .markdown-layout {
        margin-top: 25rem;
    }

    .back-button {
        font-size: 70px;
        color: var(--color-red);
        margin-left: 5rem;
    }

    .back-button::before {
        content: "←";
        display: inline-block;
        margin-right: 0.6rem;
        transition: all 0.3s ease;
    }

    .project-header {
        width: 100%;
    }

    .project__image-wrapper {
        width: 100%;
        margin: 8rem 0;
    }

    .project__image {
        width: 100%;
        height: auto;
        display: block;
    }

    .project__title {
        font-size: var(--size-xxl);
    }

    .word__category,
    .word__tools,
    .word__services {
        font-size: var(--size-l);
        font-weight: 400;
        color: var(--color-red);
    }

    .project__description {
        font-size: 45px;
        margin-bottom: 4rem;
        width: 750px;
    }

    .video_gardigan,
    .video_portal {
        width: 100%;
        height: auto;
        margin-top: 5rem;
    }

    .project__content {
        margin-top: 10rem;
    }

    .project__content :global(.intro) {
        font-size: 45px;
        margin-left: 4rem;
        width: 750px;
    }

    .project__content :global(.vraag-research) {
        font-size: 45px;
        font-weight: bold;
        margin-left: 4rem;
        width: 800px;
        margin-bottom: 7rem;
    }

    .project__content :global(.see-buttons) {
        display: flex;
        flex-direction: column;
        align-items: flex-end;
        margin-right: 5rem;
        margin-top: 5rem;
        margin-bottom: 5rem;
        gap: 1.5rem;
    }

    .project__content :global(.see-button) {
        display: block;
        font-size: 55px;
        color: var(--color-dblauw);
    }

    .project__content :global(.second-titel) {
        font-size: 85px;
        color: var(--color-dblauw);
        font-family: var(--font-header);
        margin-top: 10rem;
    }

    .project__content :global(.second-text) {
        font-size: 45px;
        margin-left: 4rem;
        width: 850px;
    }

    :global(.project__content img) {
        width: 100%;
        height: auto;
        display: block;
        justify-self: center;
        margin-top: 3rem;
    }
    :global(.frame) {
        display: block;
        justify-self: center;
    }

    .project__content :global(.project-switch) {
        display: flex;
        gap: 4rem;
        justify-content: center;
        margin-bottom: 8rem;
    }
    .project__content :global(.switch-btn) {
        background: none;
        border: 2px solid var(--color-dblauw);
        padding: 0.5rem 1.5rem;
        cursor: pointer;
        font-family: inherit;
        font-size: var(--size-ml);
        color: var(--color-dblauw);
        text-transform: uppercase;
        transition: all 0.3s ease;
    }

    .project__content :global(.switch-btn.active) {
        background-color: var(--color-dblauw);
        color: white;
        border-color: var(--color-dblauw);
    }

    .project__content :global(.project-tab) {
        display: none;
        flex-direction: column;
        margin-left: 4rem;
        margin-bottom: 4rem;
    }

    .project__content :global(.project-tab.active) {
        display: block;
    }

    @media screen and (min-width: 32em) {
        .back-button {
            font-size: 40px;
        }

        .project__title {
            font-size: var(--size-xl);
        }

        .word__category,
        .word__tools,
        .word__services {
            font-size: 40px;
        }

        .project__description {
            font-size: 30px;
        }

        .project__content {
            margin-top: 10rem;
        }

        .project__content :global(.intro) {
            font-size: 30px;
            width: 800px;
        }

        .project__content :global(.vraag-research) {
            font-size: 32px;
        }

        .project__content :global(.see-buttons) {
            margin-bottom: 10rem;
            gap: 1rem;
        }
        .project__content :global(.see-button) {
            font-size: 40px;
        }

        .project__content :global(.second-text) {
            font-size: 30px;
        }

        :global(.project__content img) {
            width: 70%;
        }

        .project__content :global(.switch-btn) {
            font-size: 40px;
        }
        .video_gardigan {
            justify-self: center;
        }
    }

    @media screen and (min-width: 41em) {
        .back-button {
            font-size: 35px;
        }

        .project__title {
            font-size: var(--size-xl);
        }

        .word__category,
        .word__tools,
        .word__services {
            font-size: 35px;
        }

        .project__description {
            font-size: 25px;
            width: 550px;
        }

        .project__content :global(.intro) {
            font-size: 25px;
            width: 700px;
            justify-self: center;
        }

        .project__content :global(.vraag-research) {
            font-size: 30px;
            width: 700px;
            justify-self: center;
        }

        .project__content :global(.see-button) {
            font-size: 35px;
        }

        .project__content :global(.second-titel) {
            margin-left: 9rem;
            font-size: var(--size-l);
        }

        .project__content :global(.second-text) {
            font-size: 25px;
            width: 720px;
            justify-self: center;
        }

        :global(.project__content img) {
            width: 70%;
        }

        .project__content :global(.switch-btn) {
            font-size: 30px;
        }

        .video_gardigan,
        .video_portal {
            width: 80%;
        }

        .portal-videos {
            display: flex;
            flex-direction: column;
            align-items: center;
        }
    }

    @media screen and (min-width: 53em) {
        .markdown-layout {
            margin-top: 4rem;
        }

        .back-button {
            font-size: 25px;
        }

        .project__title {
            font-size: var(--size-l);
        }

        .word__category,
        .word__tools,
        .word__services {
            font-size: 30px;
        }

        .project__description {
            font-size: 20px;
            width: 500px;
        }

        .project__content :global(.intro) {
            font-size: 20px;
            width: 600px;
        }

        .project__content :global(.vraag-research) {
            font-size: 25px;
            width: 600px;
        }

        .project__content :global(.see-button) {
            font-size: 30px;
            margin-right: 5rem;
        }

        .project__content :global(.second-titel) {
            margin-left: 11rem;
            font-size: var(--size-ml);
        }

        .project__content :global(.second-text) {
            font-size: 20px;
            width: 650px;
            justify-self: center;
        }

        :global(.project__content img) {
            width: 60%;
        }

        .project__content :global(.switch-btn) {
            font-size: 25px;
            margin-bottom: -4rem;
        }

        .video_gardigan,
        .video_portal {
            width: 60%;
        }
    }

    @media screen and (min-width: 64em) {
        .word__category,
        .word__tools,
        .word__services {
            font-size: 25px;
        }

        .project__description {
            font-size: 18px;
            width: 350px;
        }

        .project__content :global(.intro) {
            font-size: 18px;
            width: 550px;
        }

        .project__content :global(.vraag-research) {
            font-size: 20px;
            width: 550px;
        }

        .project__content :global(.see-button) {
            font-size: 25px;
        }

        .project__content :global(.second-titel) {
            margin-left: 14rem;
            font-size: var(--size-ml);
        }

        .project__content :global(.second-text) {
            font-size: 18px;
            width: 600px;
            justify-self: center;
        }

        :global(.project__content img) {
            width: 55%;
        }

        .project__content :global(.switch-btn) {
            font-size: 20px;
            margin-bottom: -4rem;
        }

        .video_gardigan,
        .video_portal {
            width: 50%;
        }
    }

    @media screen and (min-width: 66em) {
        .project-body-wrapper {
            display: grid;
            grid-template-columns: 1fr 1fr;
        }

        .project-info {
            grid-column: 1;
            grid-row: 1;
        }

        .project__content {
            display: contents;
        }

        .project__content :global(.project-intro) {
            grid-column: 2;
            grid-row: 1;
        }

        .project__content :global(.total) {
            grid-column: 1 / -1;
            grid-row: 2;
        }

        .total,
        .portal-videos,
        .video_gardigan {
            grid-column: 1 / -1;
        }

        .word__category,
        .word__tools,
        .word__services {
            font-size: 25px;
        }

        .project__description {
            font-size: 18px;
            width: 350px;
        }

        .project__content :global(.intro) {
            font-size: 18px;
            width: 450px;
            margin-left: 0;
        }

        .project__content :global(.vraag-research) {
            font-size: 20px;
            width: 450px;
            margin-left: 0;
        }

        .project__content :global(.see-button) {
            font-size: 25px;
        }

        .project__content :global(.second-titel) {
            margin-left: 16rem;
            font-size: var(--size-ml);
        }

        .project__content :global(.second-text) {
            font-size: 18px;
            width: 600px;
            justify-self: center;
        }

        :global(.project__content img) {
            width: 60%;
        }

        .project__content :global(.switch-btn) {
            font-size: 20px;
        }

        .video_gardigan,
        .video_portal {
            width: 50%;
        }

        .project__content :global(.subtitle) {
            margin-left: 0;
            margin-bottom: -0.5rem;
        }
        .project__content :global(.project-tab) {
            margin-top: -1rem;
        }
    }

    @media screen and (min-width: 70em) {
        .project__description {
            width: 450px;
        }

        .project__content :global(.intro) {
            width: 550px;
            margin-left: -8rem;
        }

        .project__content :global(.vraag-research) {
            width: 550px;
            margin-left: -8rem;
        }

        .project__content :global(.second-titel) {
            margin-left: 28rem;
            font-size: var(--size-ml);
        }
        .project__content :global(.total) {
            margin-top: -4rem;
        }

        .project__content :global(.second-text) {
            font-size: 18px;
            width: 600px;
            justify-self: center;
        }

        :global(.project__content img) {
            width: 60%;
        }

        .project__content :global(.switch-btn) {
            font-size: 20px;
            margin-bottom: -4rem;
        }

        .video_gardigan,
        .video_portal {
            width: 50%;
        }

        .project__content :global(.subtitle) {
            margin-left: -1rem;
            margin-bottom: -0.5rem;
        }
        .project__content :global(.project-tab) {
            margin-top: -1rem;
        }
    }
</style>
